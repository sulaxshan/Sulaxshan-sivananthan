const canvas=document.getElementById('world');const scene=new THREE.Scene();scene.fog=new THREE.FogExp2(0x050708,.035);const camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100);camera.position.set(0,1.4,8);const renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);const group=new THREE.Group();scene.add(group);const geo=new THREE.BufferGeometry();const count=220;const pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*20;pos[i*3+1]=(Math.random()-.5)*12;pos[i*3+2]=(Math.random()-.5)*20}geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const pts=new THREE.Points(geo,new THREE.PointsMaterial({color:0x20d9ff,size:.035,transparent:true,opacity:.55}));group.add(pts);const grid=new THREE.GridHelper(40,50,0x0b8199,0x0b2830);grid.position.y=-2.8;scene.add(grid);let mx=0,my=0,scroll=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5);my=(e.clientY/innerHeight-.5)});addEventListener('scroll',()=>scroll=scrollY/(document.body.scrollHeight-innerHeight));function animate(){requestAnimationFrame(animate);group.rotation.y+=.0006;camera.position.x+=(mx*.8-camera.position.x)*.025;camera.position.y+=(1.4-my*.45-camera.position.y)*.025;camera.position.z=8-scroll*2.4;grid.material.opacity=.18+scroll*.5;grid.material.transparent=true;renderer.render(scene,camera)}animate();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})